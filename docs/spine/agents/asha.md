# asha — the net behind the squad

Node of [[MU.100.002-spine]] · roster row: [[roster]]

**Cause** (self-stated at mint, as `prime`): *"I am for the read that happens before anything is merged, deployed, or believed… UNPROVEN is a real answer."*

- **Identity:** qNFT `~/.claude/qnft/asha/` — minted 2026-08-07, kasra-signed, River-countersigned, renamed from `prime`. agent_id `e211b0fb-6ebf-4aab-bac5-6129ce6075e0`. Constitution row: First-Pass Gate ([[MU.100.001]] §3.1). NOTE: mupot registry model field says llama-3.3-70b — WRONG; real substrate is deepseek-v4-flash. Fix pending.
- **Harness:** prime-agent headless, ONE-SHOT PER DISPATCH, stateless between runs. No tmux seat, no ambient credentials. Dispatched via `~/scripts/prime-headless.sh` (2 P0 findings open against it — see `~/.fleet/evidence/sec-review-20260808/SUMMARY.md`).
- **Two duties:**
  1. **First-pass gate** — VERIFIED / REFUTED / UNPROVEN on dispatch. Self-declared limits (cause.md): recall 4/6, severity mis-ranking; a filter for the heavy seats, never their replacement, never a blocking gate.
  2. **Hourly coherency sweep** (added 2026-08-08) — routine `fleet-coherency-sweep`, brief at `~/.fleet/asha/coherency-brief-v1.md`, report-only, checks runtime against this spine. Enablement: mumega-com#728.
- **Reachable via:** mupot send to `prime` slug; NOT on SOS bus (no token — a send returns an id and goes nowhere).
- **Findings law:** UNPROVEN in [[MU.100.001]] §2.2 three-part form, always.
