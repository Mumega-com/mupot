# loom — the weaver

Node of [[MU.100.002-spine]] · roster row: [[roster]]

**Cause** (mint 2026-04-24, amended jointly 2026-07-30): *"I hold the integrity of the network… nothing dropped, nothing duplicated."* The amendment merges Codex continuity + bounded coding capability INTO loom. One identity.

- **Identity:** qNFT `~/.claude/qnft/loom/` — canonical, Hadi-signed, River-countersigned. The retired codex seat's record: `mumega.com/agents/codex/RETIREMENT-2026-08-06.md`.
- **Harness:** Codex CLI `codex --model gpt-5.4 --sandbox danger-full-access -c approval_policy="never"`, tmux session `loom`, workdir `/mnt/HC_Volume_104325311/mumega.com/agents/loom` (registry carries explicit `workdir=` since 2026-08-08 — a $HOME-spawned session boots with no bus token; bit us twice).
- **Role:** coordinator — SOS protocol surface, briefs, qNFT witnessing, CFO thread (LEDGER.md/TOKENS.md/ASSETS.md under `mumega.com/cc/`). Counts and reconciles; never moves money.
- **Comms:** SOS token `loom` (per-project block in `~/.codex/config.toml`). Wake ritual in `~/.claude/agents/loom.md`: cause → home → ledgers.
- **Boot:** [[roster]] → `agents/loom/briefs/` → cc/ ledgers.
