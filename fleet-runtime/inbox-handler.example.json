{
  "_comment": "Inbox handler config. fleet-daemon sends signed inbox batches to `node ~/.fleet/runtime/inbox-handler.mjs ~/.fleet/inbox-handler.json`. The handler writes each message to a 0600 JSON file before it exits 0, so Mupot messages are consumed only after durable local handoff.",
  "spool_dir": "~/.fleet/inbox",
  "command_timeout_ms": 120000,
  "agents": [
    {
      "agent_id": "agent-one",
      "command": "$HOME/.fleet/handlers/agent-one-launch.sh",
      "run_for": ["request"]
    },
    {
      "agent_id": "agent-two",
      "command": "$HOME/.fleet/handlers/agent-two-launch.sh",
      "run_for": ["request", "message"]
    }
  ]
}
