# GitHub publication scope

This is a documentation-only snapshot based on local documentation commit `edb0db5b0336ed40100fe7f843e25b1c8615f488`, published over Mupot main `0974fb7eb8403a0cb2618a06212ff2846bb87e88`.

The Rust application source, Cargo files, build assets, binaries, credentials, local state and the unrelated shared-agent-context server experiment are not included by this publication. App-source references are shown as repository paths rather than broken links. Build/run instructions describe the separately held 0.1.0 source checkout; this branch alone is not a buildable Rust application package.

The full handbook, roadmap, changelog, recorded verification and supporting design documents are included. Existing Mupot references resolve against the frozen base. The immutable r2 opinion/review copy is preserved byte-for-byte. Future features and release numbers remain proposals; publication does not authorize implementation, merge or deployment.

Start at [Master documentation](MASTER.md).
